var nomeDoPrimeiroAluno = "Alexsandro" // estamos declarando uma variavel que esta recebendo um valor. 

console.log(nomeDoPrimeiroAluno) // estamos mostrando (Imprimindo) o valor da nossa variavel obs : sempre colocar o nome da variavel em () e sem "". 

let numeroDeAprovados =  5 // estamos declarando uma variavel que esta recebendo um valor. 

console.log(numeroDeAprovados) // estamos mostrando (Imprimindo) o valor da nossa variavel obs : sempre colocar o nome da variavel em () e sem "". 

console.clear() //estamos limpando o console.


/*
const nomeAluno = "Facebook" // estamos declarando uma variavel que é do tipo fixa, ou seja não podemos alterar ou redeclarar, em suma não podemos modifcar mais!

nomeAluno = "facebook" // aqui estamos tenando mudar o valor da variavel, isso não existe na const.

console.log(nomeAluno) // aqui estamos declarando para que mostre o valor que declaramos na variavel que a mesma recebeu

console.clear() // estamos limpando o cosole
*/


var numero1 = 2168797; // estamos declarando uma varivel e nela iremos fazer uma conta de soma

var numero2 = 3; // estamos declarando uma varivel e nela iremos fazer uma conta de soma

var soma = numero1 + numero2 // estamos declarando a variavel soma que vai receber os valores 1 e 2 e por fim soma-los. 

console.log(soma) // mostrando na tela o resultado da soma.

console.clear() //limpando todo conteudo

//Concatenar - Ato  de juntar duas ou mais strings (+)

var texto1 = "SPIDER MAN" //estamos declarando uma variavel do tipo texto,

var texto2 = " MELHOR HEROI" // estamos continuando o texto anterior,porem declarando outra variavel

console.log(texto1 + texto2) // estamos fazendo a concatenação entre os dois textos e tornando unico e fazendo o mesmo ter um sentido

console.clear() //estamos limpando todo o conteudo

var incremento = 2; // estamos declarando a variavel que esta recebendo o valor de 2


incremento+=28 // estamos redeclarando a variavel e acrescentando um valor a mais, ou seja tera uma soma entre o valor anterior e o valor atual que acabei de acrescentar

console.log(incremento)

console.clear() //estamos limpando o conteudo

var imparPar = 3%2 // estamos declarando uma variavel o programa esta tentando identifcar se o resto da divisão é Impar ou Par
console.log("se o resultado for 1 então o numero é Impar se o valor for 0 então o valor é par, esse então é o seu resultado " + imparPar) // estamos usando a concatenação para mostrar a mensagem para o usuario

console.clear() // estamos limpando o conteudo

var comparacao = 1>0 // estamos declarando uma variavel e nela estamos tentando encontrar um valor verdadeiro ou falso

console.log("se o valor foi maior ou menor que ele mesmo, retornar um valor TRUE ou FALSE,logo o resultado é :  " + comparacao ) 
console.clear() //estamos limpando o conteudo

var doisSinaisDeIgual = 2 == 3
console.log(doisSinaisDeIgual)

console.clear() //estamos limpando o conteudo
